Status: ready-for-agent

## What to build

Scaffold the Admin Web App as a Next.js application inside `apps/admin`. It should boot locally with a basic layout (nav shell, main content area) and a placeholder home route. No auth or real data yet — just confirms the app starts and the routing structure is in place.

## Acceptance criteria

- [ ] `apps/admin` runs with `next dev` and serves a page at `/`
- [ ] Basic layout component with nav shell renders
- [ ] TypeScript compiles without errors
- [ ] Linting passes

## Blocked by

- #01 Monorepo workspace scaffold
